sudo: required

services:
    - docker

before_install:
    - docker build -t imagename/docker-react  -f thirdapp/Dockerfile.dev thirdapp/

script:
    - docker run imagename/docker-react npn run test -- --coverage
